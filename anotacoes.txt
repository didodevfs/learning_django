Utilizado o ambiente virtual virtualenv
Quando fui instalar o virtualenv:
    WARNING: The script virtualenv.exe is installed in 'C:\Users\Samsung\AppData\Local\Python\pythoncore-3.14-64\Scripts' which is not on PATH.
  Consider adding this directory to PATH or, if you prefer to suppress this warning, use --no-warn-script-location.

    Acho que nem precisava instalar. Já conseguiria usar sem instalar... ?

django-admin help
    retorna lista de comando do Django

django-admin startproject nomedoprojeto .
    boa prática de nome do projeto: config ou setup
    o . faz com que não seja criada subpasta dentro do projeto

py manage.py runserver
    para subir o servidor

Alterado timezone
    De 'UTC' para 'America/Fortaleza'
    Fiz, mas, na verdade, a hora mostrada já aparentava ser a hora correta para mim

Alterado language
    De 'en-us' para 'pt-br'

Criado .env
    movido o SECRET_KEY de settings.py para .env
    diferentemente do aprendido no curso de Flaks, não foi utilizado import os, mas from pathlib import os (já estava sendo importado outras coisas do pathlib)
